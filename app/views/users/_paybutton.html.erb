<%= turbo_frame_tag "paybutton" do  %>
          <% if payment.paid == false %>
            <%= button_to "Pay All Pending", category_payment_pay_path(@category.id, payment.id), method: :put, class: 'btn btn-primary' %>
          <% else %>
            <span>Paid</span>
          <% end %>
        <% end %>